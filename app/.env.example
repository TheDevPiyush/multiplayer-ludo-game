EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=
EXPO_PUBLIC_API_URL=""

# TURN credentials are fetched from the server at runtime (GET /rtc/ice-servers).
# No client-side TURN env vars needed when using Cloudflare via the API.
